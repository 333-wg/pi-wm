# Pi-Wm v0.1.10

## 本次更新

- 自动重试默认由 2 次增加到 5 次，按 1、2、4、8、16 秒退避；完善 HTTP/2 断流、临时不可用和限流的识别，余额不足、鉴权失败及无效请求不自动重试。
- 重试过程使用单个“正在恢复连接”提示，保留次数与技术详情，刷新后恢复展示；隐藏重复中间报错，正确标记未执行的中断工具调用。
- 侧栏显示运行中会话的动态状态，切换会话后仍同步后台任务的运行、重试和结束状态。
- 优化连续工具记录折叠，支持浏览器操作、失败/中止记录和部分纯文本输出；保留交付文件及媒体的独立展示，调整计数、摘要与窄屏排版。
- Windows 应用内更新显示原生安装进度，依次提示检查、替换、安装和文件校验；校验后自动启动新版，启动失败显示诊断提示。兼容旧版静默更新参数。

## 安装与更新

- Windows x64：`Pi-Wm-0.1.10-Setup-x64.exe`。
- Mac Apple Silicon：`Pi-Wm-0.1.10-mac-arm64.dmg`；Intel：`Pi-Wm-0.1.10-mac-x64.dmg`。
- Mac ZIP 是另一种分发格式，无需与 DMG 同时下载。
- 已配置更新源的 Windows 客户端可在“设置 > 关于与更新”中检查、下载并确认重启安装。安装期间请勿重复打开应用。

## 构建与验证

- Windows 和两种 Mac 架构均基于应用提交 `23b5506`。后续提交仅调整测试和发布文档，未改变安装包中的应用代码。
- `npm run check` 和全量 `npm test` 通过；本地全量测试共 1,668 项通过、1 项既有跳过，使用 `VITEST_MAX_WORKERS=2`。最终 GitHub CI `35684438233` 通过，对应提交 `2228f6e`。
- 本次相关的 25 项浏览器回归分别通过，覆盖重试、报错展示、后台会话状态、工具折叠、更新界面、归档和分叉；报错展示另连续重复两轮通过。桌面与手机端截图已检查。
- Windows 最终 EXE 解包后逐项核对 15,781 个运行时文件，并在源码目录外通过 Chromium、PDF、canvas、SQLite、文件工作流、审批、停止、重启恢复、崩溃恢复及内嵌浏览器验证。
- Mac 原生构建与验证：GitHub Actions `35683797917`，ARM64 和 Intel 均通过；DMG、ZIP 与 SHA-256 文件已上传并交叉核对。
- Windows 真实升级：GitHub Actions `35683907945` 的普通 TEMP 与中文超长 TEMP 场景均通过，完成 `0.1.9` 到 `0.1.10` 的应用内下载、真实 NSIS 安装和自动重启，验证运行时文件、会话、工作区文件、界面偏好与更新设置保留。
- Windows EXE SHA-256：`cd08b369f0167d984a3e8b77efab738a5278ac9849d337066f5eac638358b462`。
- 已于 2026-09-22 11:54（北京时间）正式发布并设为最新版本，仅保留九个正式附件。
- 发布后，隔离的 `0.1.9` 客户端通过原有公开 GitHub 更新源发现 `0.1.10`，下载实际 EXE 并通过校验；未使用访问令牌、未替换更新源，也未在当前电脑执行安装器。

## 安装包大小

以下使用十进制 MB（1 MB = 1,000,000 字节）。

| 安装包                |      大小 |    字节数 |
| --------------------- | --------: | --------: |
| Windows x64 EXE       | 249.45 MB | 249449721 |
| Mac Apple Silicon DMG | 269.85 MB | 269854677 |
| Mac Intel DMG         | 281.19 MB | 281188636 |
| Mac Apple Silicon ZIP | 305.73 MB | 305733782 |
| Mac Intel ZIP         | 319.20 MB | 319200859 |

## 验证边界与限制

- Windows 安装包未进行代码签名；macOS 为 ad-hoc 签名，未公证，系统可能提示安全警告。
- 自动重试限于被识别为可恢复的模型请求故障，不保证上游服务恢复；额度、权限与模型可用性仍由供应商决定。
- Windows 更新进度改动不改变 macOS 的安装流程，也不宣称提供完整安装回滚。
- 原生安装器夹具测试与真实安装升级验收分别通过；前者不替代后者。
- 额外执行的旧版整页 E2E 仍有 6 项未迁移到现有界面的测试，涉及旧“目标”标签和手机端隐藏状态栏等过时假设；不宣称整个历史 E2E 套件全绿。本次相关回归与源码 CI 的通过结果单独记录。
