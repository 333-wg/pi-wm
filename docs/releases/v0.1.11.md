# Pi-Wm v0.1.11

## 本次更新

- 新增独立「定时任务」中心：可按项目和模型配置名称、描述、提示词及日程，支持编辑、启停、立即执行、删除、执行日志和历史对话。支持每日、每周、每月、一次性及固定间隔日程，按指定时区预览下一次运行。
- 定时任务由本地服务调度，同一任务不会重叠执行；错过的计划不补跑，已领取的运行仍参与故障恢复。原有会话自动化保留，并增加编辑、删除和日历日程，不会自动改成独立任务。
- 整轮任务默认不再受固定 20 分钟期限限制；模型请求连续 10 分钟没有流事件时退出该请求并按现有恢复规则处理。工具执行不计入模型静默时间。
- 改进长任务诊断，显示命令、运行时长与最近输出时间；收紧错误详情和命令显示的凭据脱敏，并隔离桌面宿主环境变量与子进程树。
- 「立即引导」可中断当前执行并优先处理新输入；「稍后跟进」等待当前任务结束，按接收顺序执行。

## 安装与更新

- Windows x64：`Pi-Wm-0.1.11-Setup-x64.exe`。应用内可通过「设置 > 关于与更新」检查、下载并确认安装。
- Mac Apple Silicon：`Pi-Wm-0.1.11-mac-arm64.dmg`；Intel：`Pi-Wm-0.1.11-mac-x64.dmg`。Mac 仍需手动替换应用，ZIP 是另一种分发格式。

## 构建与验证

- Windows 与两种 Mac 架构均基于应用提交 `b8fb427`，版本标签 `v0.1.11` 固定指向该提交。后续提交只修正升级验收脚本和发布文档，不改变安装包应用代码。
- `npm run check` 和全量 `npm test` 通过：1,844 项通过、1 项既有跳过。源码 CI `35809735622` 与验收脚本修正后的 CI `35810612402` 均通过。
- 本次相关的 20 项浏览器回归通过，覆盖独立定时任务、日历日程、旧自动化、长任务诊断和对话排队；包含 1440/390/320px 布局及真实时钟到点触发。
- Windows 最终 EXE 解包后核对 15,787 个运行时文件，并在源码目录外验证内置 Chromium、PDF、canvas、SQLite、10 个真实工作流及内嵌浏览器。浏览器门槛首轮视图等待超时，独立复测和完整安装器复测均通过，未改动安装包。
- 本机隔离的 `0.1.10` 客户端通过下载取消、损坏包拒绝、SHA-512 校验、忙碌状态拦截、安装交接和旧数据兼容检查；此检查不执行本机安装器。
- Windows 真实升级 CI `35810626268` 的普通 TEMP 与中文长 TEMP 场景均通过，完成 `0.1.10` 到 `0.1.11` 的实际 NSIS 替换、自动重启及会话、工作区、界面偏好和更新设置保留。验收脚本已兼容 `0.1.10` 的可视化安装参数，未模拟安装器执行。
- Mac 原生构建与验证 CI `35809758230` 通过。Intel 首轮制作 DMG 时遇到 runner 挂载设备占用，同一源码重跑后通过。附件经 `35811451854` 从已验证构建转入草稿，未重新构建，四个 Mac 安装文件的 SHA-256 均与 GitHub 附件摘要一致。
- Windows EXE SHA-256：`de1f2173d12145a06a155f91eaa2fe697a89582200b4335e17e4267e9a3be61d`。
- 已于 2026-09-23 10:51（北京时间）正式发布并设为最新版本；发布前移除了测试专用元数据，仅保留九个正式附件。
- 发布后，隔离的 `0.1.10` 客户端通过原有公开 GitHub 更新源发现 `0.1.11`，下载实际 EXE 并通过 SHA-512 校验；未使用访问令牌、未替换更新源，也未在当前电脑执行安装器。

## 安装包大小

以下使用十进制 MB（1 MB = 1,000,000 字节）。

| 安装包                |      大小 |    字节数 |
| --------------------- | --------: | --------: |
| Windows x64 EXE       | 249.47 MB | 249470786 |
| Mac Apple Silicon DMG | 269.95 MB | 269947395 |
| Mac Intel DMG         | 281.09 MB | 281092667 |
| Mac Apple Silicon ZIP | 305.76 MB | 305760955 |
| Mac Intel ZIP         | 319.23 MB | 319228045 |

## 验证边界与限制

- 独立定时任务固定使用完全访问权限，工作项目指定执行目录，但不是文件系统隔离。仅为信任的任务配置提示词；普通聊天的权限设置不受影响。
- 计划只有在本地服务运行、电脑唤醒时触发；错过的时间不补跑。尚不支持自定义五段 Cron、任务级通知或 Git worktree 隔离。
- 模型流静默保护不保证第三方供应商可用；真实渠道的长期稳定性未由模拟测试覆盖。
- Windows 安装包未签名；macOS 为 ad-hoc 签名、未经公证。系统可能提示未知发布者或拦截安装。
